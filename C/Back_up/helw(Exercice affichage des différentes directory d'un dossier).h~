#ifndef DEF_HELW
#define DEF_HELW

typedef struct dirent dirent;
typedef struct List List;
typedef struct CaseList CaseList;
struct List
{
	int length_list;
	CaseList *first;
};
struct CaseList
{
	char* option_name;
	CaseList *next;
};
#define ENTER 10

#endif

